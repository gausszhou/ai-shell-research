# AI SHELL NODEJS

```prompt
使用 nodejs 构建一个 cli 程序，作用是在用户和命令解释器之间建立一座桥梁，要求如下
1. 使用和当前相同的 shell 执行用户的命令 
3. 过滤掉 PTY 输出的提示符部分，使用我们自己的提示符
4. 在我们自己的提示符之前加上 [AI SHELL] 标签 
5. 工作目录要显示完整路径
6. 工作目录要能同步更新展示
7. 不要做任何多余的功能拓展，比如危险命令拦截
```
